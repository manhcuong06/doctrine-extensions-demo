<form method="post" action="{{ path('loggable_delete', {'id': loggable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loggable.id) }}">
    <button class="btn">Delete</button>
</form>
