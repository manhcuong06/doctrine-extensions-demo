{% extends 'base.html.twig' %}

{% block title %}Softdeleteable{% endblock %}

{% block body %}
    <h1>Softdeleteable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ softdeleteable.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ softdeleteable.title }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ softdeleteable.deletedAt ? softdeleteable.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('softdeleteable_index') }}">back to list</a>

    <a href="{{ path('softdeleteable_edit', {'id': softdeleteable.id}) }}">edit</a>

    {{ include('softdeleteable/_delete_form.html.twig') }}
{% endblock %}
