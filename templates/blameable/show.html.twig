{% extends 'base.html.twig' %}

{% block title %}Blameable{% endblock %}

{% block body %}
    <h1>Blameable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blameable.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ blameable.title }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ blameable.createdBy }}</td>
            </tr>
            <tr>
                <th>UpdatedBy</th>
                <td>{{ blameable.updatedBy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blameable_index') }}">back to list</a>

    <a href="{{ path('blameable_edit', {'id': blameable.id}) }}">edit</a>

    {{ include('blameable/_delete_form.html.twig') }}
{% endblock %}
