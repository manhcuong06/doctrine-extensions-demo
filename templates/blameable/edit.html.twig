{% extends 'base.html.twig' %}

{% block title %}Edit Blameable{% endblock %}

{% block body %}
    <h1>Edit Blameable</h1>

    {{ include('blameable/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('blameable_index') }}">back to list</a>

    {{ include('blameable/_delete_form.html.twig') }}
{% endblock %}
