<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="shortcut icon" type="image/svg+xml" href="/favicon.svg">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <a href="{{ path('home_index') }}">Home</a>
        <a href="{{ path('blameable_index') }}">Blameable</a>
        <a href="{{ path('sluggable_index') }}">Sluggable</a>
        <a href="{{ path('softdeleteable_index') }}">Softdeleteable</a>
        <a href="{{ path('sortable_index') }}">Sortable</a>
        <a href="{{ path('timestampable_index') }}">Timestampable</a>
        <a href="{{ path('tree_index') }}">Tree</a>

        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
