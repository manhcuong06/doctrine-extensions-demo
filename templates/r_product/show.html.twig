{% extends 'base.html.twig' %}

{% block title %}RProduct{% endblock %}

{% block body %}
    <h1>RProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ r_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ r_product.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('r_product_index') }}">back to list</a>

    <a href="{{ path('r_product_edit', {'id': r_product.id}) }}">edit</a>

    {{ include('r_product/_delete_form.html.twig') }}
{% endblock %}
