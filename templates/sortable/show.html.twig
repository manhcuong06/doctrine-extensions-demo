{% extends 'base.html.twig' %}

{% block title %}Sortable{% endblock %}

{% block body %}
    <h1>Sortable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sortable.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sortable.name }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ sortable.position }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ sortable.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sortable_index') }}">back to list</a>

    <a href="{{ path('sortable_edit', {'id': sortable.id}) }}">edit</a>

    {{ include('sortable/_delete_form.html.twig') }}
{% endblock %}
