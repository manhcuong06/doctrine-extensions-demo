<form method="post" action="{{ path('timestampable_delete', {'id': timestampable.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ timestampable.id) }}">
    <button class="btn">Delete</button>
</form>
